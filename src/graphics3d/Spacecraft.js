// Spacecraft implementation removed as part of revert. Keeping an empty module to avoid import errors
// If you want the file deleted entirely, we can remove it from the repo in a follow-up step.
export default null;
